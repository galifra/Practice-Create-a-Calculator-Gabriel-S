Function getAbsoluteValue(number) {
    if (number < 0){
        return -number; //Flip it to positive
    }
    Return number; // Already positive
}
Function CalculatePower(Base, exponent) {
    return base ** exponent;
}
